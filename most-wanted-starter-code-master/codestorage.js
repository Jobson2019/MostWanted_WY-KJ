function displayFamily(famInfo, data){
  // let famInfoAlert = 
  // " Direct Family: " + 
  data.filter (function(el) {
    if(el.id === famInfo.parents){
      return true;
    }
    else {
      return false;
    }
  }
    )
  if {
    id === famInfo.parents
  } 

  else {
    return false;
  }

  alert(famInfoAlert)
}